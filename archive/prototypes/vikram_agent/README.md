# Vikram Agent Prototype Archive

This archive preserves the surviving local `vikram_agent` workspace material recovered from the founder's earlier prototype workspace.

## What Survived

The available local copy contains agent identity, memory, and workspace bootstrap files:

- `workspaces/agents/lead/AGENTS.md`
- `workspaces/agents/lead/BOOTSTRAP.md`
- `workspaces/agents/lead/HEARTBEAT.md`
- `workspaces/agents/lead/IDENTITY.md`
- `workspaces/agents/lead/MEMORY.md`
- `workspaces/agents/lead/SOUL.md`
- `workspaces/agents/lead/TOOLS.md`
- `workspaces/agents/lead/USER.md`
- `workspaces/agents/lead/memory/2026-04-25.md`

The nested `.git` directory and `.openclaw` runtime state were not copied into this archive.

## What Did Not Survive Here

The earlier Python pipeline prototype that was previously referenced as `vikram_agent` was not found in this workspace during the recovery pass.

This archive should therefore be treated as historical context, not as an implementation source.

## Importance For Vikram

This material is low to medium importance.

It is useful for:

- understanding early assistant identity and memory experiments
- preserving context around the older Vikram-style agent concept
- comparing personal-assistant workspace ideas against Vikram's current founder-operated engineering-system direction

It is not required for:

- the Go host daemon
- the Python LangGraph orchestrator
- founder approval flow
- merge-readiness workflow
- current Vikram v1 architecture

If the old Python prototype is needed again, it should be recreated from the current Vikram architecture and historical notes rather than treated as a production base.
